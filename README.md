# matrix
Matrix representation in linux term just as in "The Matrix" film

# Install dependencies:
`make install`

# Run matrix
`make`

`./tm`

# Debug matrix

`make debug`
`gdb dtm`

# Clean-up
`make clean`
